# Dependencies
node_modules/
reels-composer/node_modules/

# Python
__pycache__/
*.pyc

# Local secrets (optional — or use env vars)
build/secrets_local.py

# Generated n8n workflows. The builder inlines the OpenRouter, Tavily,
# ElevenLabs, Cartesia and S3 keys straight into the Code nodes, so these files
# are secrets. Rebuild with `python3 build/build_workflow.py`.
automations/**/*.json
working_automations/

# Local scratch: old workflow exports and paste-into-n8n snippets, several of
# which still have live keys pasted into them.
archive/

# Local test artifacts
.tmp-test/

# OS / editor
.DS_Store
.env
.env.*

# Logs
*.log
npm-debug.log*
